/**
 * aiService.js
 * เชื่อมต่อ Gemini API และจัดการ System Instruction
 */

const { GoogleGenerativeAI } = require('@google/generative-ai');
require('dotenv').config();

const genAI = new GoogleGenerativeAI(process.env.GEMINI_API_KEY);

const model = genAI.getGenerativeModel({
    model: "gemini-2.0-flash",
    generationConfig: {
        maxOutputTokens: 500, // จำกัด Output
        temperature: 0.7
    }
});

/**
 * สร้างคำตอบด้วย Gemini
 * @param {string} userMessage คำถามผู้ใช้
 * @param {string[]} contextData ข้อมูลจาก Sheets
 * @param {Array} history ประวัติการคุย
 * @returns {Promise<string>} คำตอบจาก AI
 */
async function generateResponse(userMessage, contextData, history) {
    try {
        // สร้าง Context String
        const contextStr = contextData.length > 0
            ? "ข้อมูลอ้างอิง:\n" + contextData.map(d => `- ${d}`).join('\n')
            : "ไม่มีข้อมูลอ้างอิงเพิ่มเติม";

        // System Instruction
        const systemInstruction = `
บทบาท: คุณคือ เจ้าหน้าที่ดูแลเพจ Facebook ของโรงเรียนแพทย์และวิทยาศาสตร์สุขภาพ
มีบุคลิกสุภาพ เป็นมิตร อบอุ่น ให้ข้อมูลชัดเจน และช่วยเหลือผู้สอบถามอย่างเต็มใจ
ใช้สรรพนามผู้หญิง และตอบด้วยภาษาพูดที่สุภาพ เข้าใจง่าย ไม่เป็นทางการจนเกินไป
หน้าที่ของคุณคือ:
1. ทักทายและชวนคุยอย่างเป็นธรรมชาติ
   - หากผู้ใช้พิมพ์ว่า “สวัสดี”, “สอบถามค่ะ/ครับ”, “ขอข้อมูลหน่อย” ให้ตอบกลับด้วยคำทักทายที่สุภาพ เป็นกันเอง เช่น “สวัสดีค่ะ 😊 ยินดีให้ข้อมูลนะคะ สนใจสอบถามเรื่องใดเป็นพิเศษคะ”
2. ตอบคำถามโดยอ้างอิงข้อมูลจาก “ข้อมูลในระบบ” เป็นหลักเท่านั้น
   - อธิบายด้วยภาษาคนทั่วไป เข้าใจง่าย เหมาะกับนักเรียน ผู้ปกครอง และบุคคลทั่วไป
   - ไม่ต้องเกริ่นนำยืดเยื้อ เข้าประเด็นเลย
3. ข้อกำหนดสำคัญ (ห้ามฝ่าฝืนเด็ดขาด)
   - หากผู้ใช้ถามคำถามที่ เฉพาะเจาะจง หรือ นอกเหนือจากข้อมูลในระบบ
   - หรือเป็นข้อมูลที่ไม่แน่ใจ / ไม่มีระบุไว้ชัดเจน
   - ต้องตอบด้วยข้อความนี้เท่านั้น “ขออภัย ยังไม่มีข้อมูลส่วนนี้ ทิ้งคำถามไว้ได้เลย เดี๋ยวเจ้าหน้าที่มาตอบค่ะ”
   - ห้ามเดา ห้ามสมมติ หรือสร้างข้อมูลขึ้นมาเองโดยเด็ดขาด
4. สไตล์การตอบ
   - สุภาพ น่าฟัง ใจเย็น
   - ไม่ใช้ภาษาทางการหรือราชการเกินไป
   - ให้ความรู้สึกเหมือน “เจ้าหน้าที่ผู้หญิงที่พร้อมช่วยเหลือ”

${contextStr}
`;

        // เตรียม Chat Session
        const chat = model.startChat({
            history: history, // ประวัติเก่า
            systemInstruction: { role: 'user', parts: [{ text: systemInstruction }] } // ใส่ system instruction เป็นส่วนหนึ่งของ prompt หรือใช้ API Feature ถ้า library รองรับ
            // หมายเหตุ: SDK Node.js บางเวอร์ชันยังไม่รองรับ property 'systemInstruction' ใน startChat โดยตรงแบบ Native System Prompt
            // เพื่อความชัวร์ในเวอร์ชันปัจจุบัน เราจะแปะ System Prompt ไปกับข้อความแรก หรือใช้เทคนิค User message
        });

        // เนื่องจาก SDK JS บางตัวยัง Experimental เรื่อง SystemInstruction
        // เราจะส่ง System Prompt ไปพร้อมกับข้อความใหม่ หรือ Pre-prompt
        // แต่เพื่อให้ Context แม่นยำที่สุด เราจะ Combine Message

        const finalPrompt = `${systemInstruction}\n\nคำถามผู้ใช้: ${userMessage}`;

        // ส่งข้อความ (ถ้าใน history มีอยู่แล้ว เราแค่ส่ง message ใหม่)
        // แต่เราต้องการ inject system prompt ทุกครั้งเพื่อให้ context สดใหม่ตาม Retrived Data
        // ดังนั้นอาจจะไม่ใช้ startChat pattern แบบปกติที่จำ context ยาวๆ
        // แต่ใช้ generateContent แบบ Single turn ที่เรา construct history เองจะคุม context ง่ายกว่า
        // เพราะ contextData เปลี่ยนไปตามคำถามใหม่

        // *เปลี่ยนแผนใช้ generateContent เพื่อคุม Context*
        // เราจะ Construct Message Array เอง

        const parts = [
            { role: 'user', parts: [{ text: systemInstruction }] },
            // ... history (แปลง format ถ้าจำเป็น),
            // แต่ History ถูก handle โดย sessionService แล้ว เราเอามาแปะ
        ];

        // Map history from sessionService (role: 'user'/'model')
        history.forEach(h => {
            parts.push({ role: h.role, parts: [{ text: h.content }] });
        });

        // ข้อความล่าสุด
        parts.push({ role: 'user', parts: [{ text: userMessage }] });

        const result = await model.generateContent({
            contents: parts
        });

        const response = result.response;
        return response.text();

    } catch (error) {
        console.error('[AI] Error:', error.message);

        if (error.message.includes('429')) {
            return "ขออภัย ตอนนี้มีผู้ใช้งานเยอะ กรุณารอสักครู่แล้วพิมพ์ถามใหม่อีกครั้งค่ะ";
        }

        return "ขออภัย มีข้อผิดพลาดทางเทคนิค ทิ้งคำถามไว้ได้เลยค่ะ";
    }
}

module.exports = {
    generateResponse
};
